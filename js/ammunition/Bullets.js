/**
 * Bullet class that generates or draws bullets for player enemy
 */
export default class Bullet {
    /**
     * 
     * @param {Object} player player object or enemy so that it can shoot based on player 
     */
    constructor() {
    }
    draw(ctx) {
    }
    update() {

    }
}